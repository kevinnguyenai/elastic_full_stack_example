input {
	beats {
		port => 5044
                codec => "json_lines"
	}

	tcp {
		port => 5000
	}
}

## Add your filters / logstash plugins configuration here

filter {
	mutate {
		remove_field => ["source","host","@version","prospector","input","beat"]
	}
}


output {
	elasticsearch {
		hosts => ['elasticsearch:9200']
		user => '${ES_USERNAME:elastic}'
		password => '${ES_PASSWORD:Tuanna!@#123}'
	}
}
